---
import Buttons from "./Buttons.astro";
import Toast from "./Toast.astro";
---

<style>
  :root{
    --green-dot: #64C589;
    --dark: #000005;
    --light: #EAECF0;
    --light-gray: #F6F6F6;
    --gray: #A4AAB6;
    --radius-full: 100%;
  }
  body{
    padding: 0 0.5rem !important;
    background-color: var(--light-gray);
  }

  .profile-pic{
    border-radius: var(--radius-full);
    width: 300px;
    height: 300px;
    border: 4px solid var(--light);
  }
  .profile-pic > img {
    width: 100%;
    height: 100%;
    border-radius: var(--radius-full);
  }
  /* PLAIN CSS IS BETTER THAN TAILWIND */
  /*TODO: i should change all explicit coloring with tailwind custom coloring component*/

</style>
<main class="m-auto max-w-[1200px] mt-2 rounded bg-white shadow-md p-4 ">
  <Toast status="success" title="Email copied to clipboard!"/>
  <section>
    <div class="flex justify-between items-center">
      <Buttons title="Full Stack Engineer" type="decoration" design="default"/>
      <Buttons title="Open To Work" type="decoration" design="available"/>
    </div>
    <div class="w-full flex flex-col gap-4 mt-8 items-center p-2 lg:flex-row-reverse lg:justify-between">
      <!-- Profile Picture -->
      <div class="bg-white profile-pic p-2 flex items-center justify-center">
        <img alt="profile-pic" src="/profile-pic.svg"/>
      </div>

      <!-- Text Content -->
      <div class="h-full flex flex-col gap-4">
        <h1 class="font-bold text-2xl">I'm Eliuth</h1>
        <p class="relative text-sm text-gray-500">Full Stack Developer based on Galicia, Spain 🇪🇸
        <br /> Currently working on   
          <span class="relative inline-block before:absolute before:-inset-1 before:block before:-skew-y-2 before:bg-midnight-void">
            <span class="relative font-bold dark:text-white">Mens Nova</span>
        </span>
        as Frontend Specialist
        </p>
        <!--- Buttons --->
        <div class="flex gap-4 ">
          <Buttons title="Hire me" type="link" design="dark" link="http://localhost:4321/"/>
          <Buttons title="Copy Email" type="default" design="outlined"/>
        </div>
      </div>
   </div>
  </section>
</main>
